---
import daysJs from 'dayjs'

function calculateDates(startDate: string, weeks: number) {
  const baseDate = daysJs(startDate)
  const resultDate = baseDate.add(weeks, 'week')
  return resultDate.format('YYYY-MM-DD')
}

export { calculateDates}
---
